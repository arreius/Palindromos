import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.palindromosProgra3.app',
  appName: ' Palindromos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
